server {
  listen 3000;
  root /usr/share/nginx/html;
  location / { try_files $uri /index.html; }
  location /api/ {
    proxy_pass http://cofiswarm-gateway:3002;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
  }
}
